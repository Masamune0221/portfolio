---
const { lang_name, icon } = Astro.props;
---

<div class="flex flex-row items-center gap-4">
  <img
    src={`${import.meta.env.BASE_URL}${icon.replace(/^\//, "")}`}
    alt={lang_name}
    class="w-8 h-8"
  />
  <p>{lang_name}</p>
</div>
